{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4">Garde Robe</h1>

    {% for clothingItem in clothingItems %}
        <div class="border p-4 rounded-lg shadow-lg mb-4 bg-white">
            <h2 class="text-lg font-semibold">{{ clothingItem.name }}</h2>

            {% if clothingItem.image %}
                <img src="{{ asset(clothingItem.image) }}" alt="{{ clothingItem.name }}" class="w-32 h-32 object-cover mt-2">
            {% endif %}
        </div>
    {% else %}
        <p>Aucun vêtement disponible.</p>
    {% endfor %}
{% endblock %}
